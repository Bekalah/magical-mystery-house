# npm publishing configuration
# Public access for all packages
access=public

# Registry
registry=https://registry.npmjs.org/

# Save exact versions
save-exact=false

# Package lock
package-lock=false
