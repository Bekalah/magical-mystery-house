/**
 * Enhanced Image Processor for Professional Book Art Extraction
 *
 * Professional-grade image processing for extracting art elements from books:
 * - Advanced pattern recognition for artistic elements
 * - Professional color palette extraction with cultural context
 * * Sacred geometry detection and extraction
 * - Typography and lettering recognition
 * - Style analysis with art historical context
 * - Vector path extraction for redesigning
 * - Quality validation for professional use
 *
 * @author Rebecca Respawn (International Reiki Master)
 * @version 2.0.0 - Professional Edition
 */
import { ProcessedImage, ColorPalette, StyleAnalysis } from '../types/extraction-types';
export declare class ImageProcessor {
    private sacredSymbolDatabase;
    private mathematicalSymbols;
    private artPatterns;
    /**
     * Detect images in a page
     */
    detectImages(imagePath: string): Promise<any[]>;
    /**
     * Extract art elements for professional redesigning
     * This is the core method for pulling real art from books
     */
    extractArtElements(imageData: Uint8Array, style?: string): Promise<any[]>;
    /**
     * Extract vector paths for tracing and recreating artwork
     */
    extractVectorPaths(imageData: Uint8Array): Promise<any[]>;
    /**
     * Professional color palette extraction with art historical context
     */
    extractProfessionalColorPalette(imageData: Uint8Array): Promise<ColorPalette[]>;
    /**
     * Process detected image with domain-specific optimization
     */
    processImage(image: any): Promise<ProcessedImage>;
    /**
     * Extract color palette from image
     */
    extractColorPalette(imageData: Uint8Array): Promise<ColorPalette[]>;
    /**
     * Analyze style of image
     */
    analyzeStyle(imageData: Uint8Array): Promise<StyleAnalysis>;
    /**
     * Detect sacred symbols in image
     */
    detectSacredSymbols(imagePath: string): Promise<any[]>;
    /**
     * Detect technical diagrams
     */
    detectTechnicalDiagrams(imagePath: string): Promise<any[]>;
    /**
     * Process technical diagram
     */
    processTechnicalDiagram(diagram: any): Promise<ProcessedImage>;
    /**
     * Detect artistic elements
     */
    detectArtisticElements(imagePath: string): Promise<any[]>;
    /**
     * Process artwork
     */
    processArtwork(artwork: any): Promise<ProcessedImage>;
    /**
     * Analyze artistic style
     */
    analyzeArtisticStyle(imageData: Uint8Array): Promise<StyleAnalysis>;
    private calculateResolution;
    private determineColorSpace;
    private inferDomainFromImage;
    private generateColorPalette;
    private calculateColorHarmony;
    private analyzeAccessibility;
    private detectArtisticPatterns;
    private extractArtisticPatterns;
    private extractTextureElements;
    private extractSymbolicElements;
    private detectOutlinePaths;
    private extractGeometricShapes;
    private extractDecorativeElements;
    private extractTypographyPaths;
    private analyzeDominantColors;
    private analyzeColorHarmony;
    private identifyArtMovement;
    private getArtHistoricalContext;
    private getCulturalColorContext;
    private getPsychologicalColorEffect;
    private getProfessionalColorUse;
    private validatePrintColors;
    private validateWebColors;
    private validateBrandColors;
    private analyzeAccessibilityForArt;
    private calculateProfessionalGrade;
    private simpleHash;
}
//# sourceMappingURL=imageProcessor.d.ts.map