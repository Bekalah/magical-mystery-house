/**
 * styleFusion
 *
 * @package @cathedral/brain
 */
export declare class StyleFusion {
    fourierAnalysis(imageData: Float32Array): Promise<{
        dominant: never[];
        phase: never[];
        magnitude: never[];
    }>;
    goldenRatioComposition(width: number, height: number): {
        points: {
            x: number;
            y: number;
        }[];
    };
    generateFractal(type: string, iterations?: number): never[];
}
//# sourceMappingURL=styleFusion.d.ts.map