/**
 * Tests for readme renderer.
 * NOTE: This project’s test runner will be inferred from package.json (Jest/Mocha/Vitest).
 * These tests are written using Jest-style APIs (describe/it/expect), which are also compatible with Vitest.
 * If Mocha/Chai is used, adapt `expect` imports accordingly.
 */
